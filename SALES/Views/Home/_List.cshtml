@model List<SALES.Entities.Category>

@foreach (var item in Model)
{
    <div class="nav-item">
        <a class="nav-link" href="~/san-pham/loai-san-pham/@item.Id" data-danh-muc-id="@item.Id">@item.Name</a>
    </div>
    
}
